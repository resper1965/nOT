# Vercel ignore file
# Files to ignore during deployment

# Local development files
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build outputs (handled by Vercel)
.next
out
dist

# Dependencies (handled by Vercel)
node_modules

# Git
.git
.gitignore

# Documentation (optional - remove if not needed in deployment)
docs/
*.md
!README.md

# Scripts (optional - remove if needed in deployment)
scripts/

# Docker (not needed for Vercel)
Dockerfile*
docker-compose*.yml
.dockerignore

